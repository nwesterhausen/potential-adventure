<div class="card shadow-lg">
  <div class="card-body">
    <div class="card-title text-base">
        <span data-tip="{{dwarf.TranslatedName}}" class="tooltip" >{{dwarf.Name}}</span>
        <div class="badge badge-sm">{{dwarf.JoinedAt}}</div>    
        <div class="badge badge-sm badge-primary">{{ dwarf.Profession }}</div>
    </div>

    {% include "components/quote-line", quote: dwarf.Quote %}

    <span class="font-semibold">Relationships</span>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-1 text-black">
      {% include "components/dwarf-relationships", dwarf: dwarf %}
    </div>
  </div>
</div>
